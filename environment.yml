# run: conda env create --file environment.yml
name: mrcnn
dependencies:
  - plantcv
  - opencv<4, >=3.4
  - numpy==1.15.1
  - tensorflow>=1.3.0
  - keras>=2.0.8
  - pip:
    - git+https://github.com/matterport/Mask_RCNN.git
channels:
  - defaults
  - conda-forge
  - bioconda
